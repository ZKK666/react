# 05 - 事件系统和合成事件

## 核心概念

### 1. 什么是合成事件？

**合成事件（SyntheticEvent）** 是 React 对原生事件的跨浏览器封装。

**为什么需要合成事件？**
1. **跨浏览器兼容**：抹平不同浏览器的事件差异
2. **性能优化**：事件委托，减少内存占用
3. **统一的事件接口**：提供一致的API
4. **与 React 更新机制集成**：事件优先级调度

```javascript
// React 合成事件
<button onClick={(e) => {
  e.preventDefault();        // 阻止默认行为
  e.stopPropagation();       // 阻止冒泡
  console.log(e.nativeEvent); // 访问原生事件
}}>
  Click me
</button>
```

### 2. React 17 事件系统的变化

**React 16 及之前**：
- 事件委托到 `document`
- 所有事件都在 document 上监听

**React 17**：
- 事件委托到 React 树的**根容器**（root）
- 支持多个 React 版本共存

```javascript
// React 16
document.addEventListener('click', ...) // 所有事件在 document 上

// React 17
const root = document.getElementById('root');
root.addEventListener('click', ...) // 事件在 root 容器上
```

**优势**：
1. **渐进式升级**：可以在同一页面使用不同 React 版本
2. **更容易与其他库集成**：不会影响 document 上的事件
3. **更安全**：`e.stopPropagation()` 不会影响 document 上的监听器

## 事件注册和监听

### 事件注册流程

```javascript
// 1. 组件中定义事件处理器
<button onClick={handleClick}>Click</button>

// 2. React 在 Fiber completeWork 阶段注册事件
// packages/react-dom/src/events/DOMPluginEventSystem.js

function listenToNativeEvent(
  domEventName: string,
  isCapturePhaseListener: boolean,
  target: EventTarget,
): void {
  // 检查是否已注册
  if (!eventTargetForListening[listenerKey]) {
    // 注册原生事件监听器
    const listener = createEventListenerWrapper(
      target,
      domEventName,
      eventSystemFlags,
    );

    // 添加到根容器
    if (isCapturePhaseListener) {
      target.addEventListener(domEventName, listener, true);  // 捕获阶段
    } else {
      target.addEventListener(domEventName, listener, false); // 冒泡阶段
    }

    eventTargetForListening[listenerKey] = true;
  }
}
```

### 事件触发流程

```
用户点击
  ↓
原生事件在根容器触发
  ↓
dispatchEvent（React 的事件分发器）
  ↓
从目标 Fiber 向上收集事件处理器（模拟事件冒泡）
  ↓
创建合成事件对象
  ↓
按顺序执行事件处理器
  ↓
释放合成事件对象（对象池优化）
```

```javascript
// packages/react-dom/src/events/DOMPluginEventSystem.js

function dispatchEvent(
  domEventName: string,
  eventSystemFlags: EventSystemFlags,
  nativeEvent: Event,
): void {
  // 1. 找到触发事件的 DOM 节点
  const nativeEventTarget = getEventTarget(nativeEvent);

  // 2. 找到对应的 Fiber 节点
  const targetFiber = getClosestInstanceFromNode(nativeEventTarget);

  // 3. 收集事件处理器路径（从目标到根）
  const dispatchQueue = [];
  extractEvents(
    dispatchQueue,
    domEventName,
    targetFiber,
    nativeEvent,
    eventSystemFlags,
  );

  // 4. 执行事件处理器
  processDispatchQueue(dispatchQueue, eventSystemFlags);
}

// 收集事件路径
function extractEvents(
  dispatchQueue: Array<DispatchEntry>,
  domEventName: string,
  targetFiber: Fiber | null,
  nativeEvent: Event,
  eventSystemFlags: EventSystemFlags,
): void {
  const reactEventName = topLevelEventsToReactNames.get(domEventName);

  let fiber = targetFiber;
  const dispatchListeners = [];
  const dispatchInstances = [];

  // 从目标节点向上遍历，收集事件处理器
  while (fiber !== null) {
    const { tag, stateNode } = fiber;

    if (tag === HostComponent && stateNode !== null) {
      const listener = getListener(fiber, reactEventName);
      if (listener != null) {
        dispatchListeners.push(listener);
        dispatchInstances.push(fiber);
      }
    }

    fiber = fiber.return;  // 向上遍历
  }

  if (dispatchListeners.length > 0) {
    const event = new SyntheticEvent(/* ... */);
    dispatchQueue.push({
      event,
      listeners: dispatchListeners,
    });
  }
}
```

## 合成事件对象

```javascript
// 合成事件的结构
interface SyntheticEvent<T = Element> {
  // 事件类型
  type: string;

  // 原生事件对象
  nativeEvent: Event;

  // 当前触发事件的元素
  currentTarget: EventTarget & T;

  // 最初触发事件的元素
  target: EventTarget;

  // 事件冒泡和默认行为控制
  bubbles: boolean;
  cancelable: boolean;
  defaultPrevented: boolean;
  eventPhase: number;
  isTrusted: boolean;

  // 方法
  preventDefault(): void;
  stopPropagation(): void;
  persist(): void;  // React 17 不再需要
}
```

### 事件池优化（React 16）

**React 16** 使用事件池优化性能：

```javascript
// React 16：事件对象会被重用
function handleClick(e) {
  console.log(e.type);  // "click"

  setTimeout(() => {
    console.log(e.type);  // null! 事件对象已被清空
  }, 100);
}

// 解决方案：persist()
function handleClick(e) {
  e.persist();  // 从池中移除，不会被清空

  setTimeout(() => {
    console.log(e.type);  // "click"
  }, 100);
}
```

**React 17**：移除了事件池

```javascript
// React 17：不需要 persist()
function handleClick(e) {
  setTimeout(() => {
    console.log(e.type);  // "click" ✅
  }, 100);
}
```

## 事件优先级

React 根据事件类型设置不同的优先级：

```javascript
// packages/react-dom/src/events/ReactDOMEventListener.js

// 离散事件（discrete）：用户交互，高优先级
const discreteEvents = [
  'click',
  'keydown',
  'keyup',
  'input',
  'change',
  // ...
];

// 连续事件（continuous）：持续触发，中等优先级
const continuousEvents = [
  'drag',
  'dragover',
  'mouseMove',
  'touchMove',
  'scroll',
  // ...
];

// 根据事件类型设置优先级
function getEventPriority(domEventName: string): EventPriority {
  switch (domEventName) {
    case 'click':
    case 'keydown':
    case 'input':
      return DiscreteEventPriority;  // 最高优先级

    case 'drag':
    case 'mousemove':
      return ContinuousEventPriority;  // 中等优先级

    default:
      return DefaultEventPriority;  // 默认优先级
  }
}
```

## 高频面试题

### Q1: 什么是合成事件？为什么需要合成事件？

**答案**：

合成事件是 React 对原生浏览器事件的跨浏览器封装。

**优势**：
1. **跨浏览器兼容**：统一不同浏览器的事件对象接口
2. **性能优化**：事件委托，减少内存占用
3. **自动内存管理**：React 17 之前使用事件池
4. **与 React 更新机制集成**：支持事件优先级调度

```javascript
// 原生事件：不同浏览器有差异
document.addEventListener('click', (e) => {
  const target = e.target || e.srcElement;  // IE 兼容
});

// React 合成事件：统一接口
<div onClick={(e) => {
  const target = e.target;  // 所有浏览器一致
}}>
```

### Q2: React 17 的事件系统有什么变化？

**答案**：

**主要变化**：

**1. 事件委托位置改变**
```javascript
// React 16：委托到 document
document.addEventListener('click', reactEventHandler);

// React 17：委托到 root 容器
const root = document.getElementById('root');
root.addEventListener('click', reactEventHandler);
```

**好处**：
- 支持多个 React 版本共存
- 更容易与其他库集成
- `e.stopPropagation()` 更符合预期

**2. 移除事件池**
```javascript
// React 16：需要 persist()
onClick={(e) => {
  e.persist();
  setTimeout(() => console.log(e.type), 100);
}}

// React 17：不需要 persist()
onClick={(e) => {
  setTimeout(() => console.log(e.type), 100);  // 直接可用
}}
```

**3. 对齐浏览器行为**
- `onScroll` 不再冒泡（与浏览器一致）
- `onFocus` 和 `onBlur` 使用原生的 `focusin` 和 `focusout`
- 捕获阶段事件更符合浏览器行为

### Q3: 如何阻止事件冒泡？

**答案**：

**在 React 中**：
```javascript
<div onClick={() => console.log('parent')}>
  <button onClick={(e) => {
    e.stopPropagation();  // 阻止冒泡到父组件
    console.log('child');
  }}>
    Click
  </button>
</div>
// 点击按钮：只输出 "child"
```

**注意**：React 17 之前，`e.stopPropagation()` 不能阻止 document 上的原生事件：

```javascript
// React 16
document.addEventListener('click', () => {
  console.log('document');  // 仍然会执行！
});

<button onClick={(e) => {
  e.stopPropagation();  // 只阻止 React 事件冒泡
  console.log('button');
}}>

// React 17：可以阻止（因为事件在 root 上）
```

**阻止原生事件冒泡**：
```javascript
onClick={(e) => {
  e.nativeEvent.stopImmediatePropagation();  // 阻止所有事件
}}
```

### Q4: 合成事件与原生事件的执行顺序？

**答案**：

```javascript
// React 16
componentDidMount() {
  // 原生事件
  document.addEventListener('click', () => {
    console.log('1. document 原生');
  });

  this.button.addEventListener('click', () => {
    console.log('2. button 原生');
  });
}

render() {
  return (
    <button
      ref={el => this.button = el}
      onClick={() => console.log('3. button React')}
    >
      <span onClick={() => console.log('4. span React')}>
        Click
      </span>
    </button>
  );
}

// 点击 span，输出顺序：
// 2. button 原生        （捕获阶段）
// 4. span React         （React 事件冒泡）
// 3. button React
// 1. document 原生      （原生事件冒泡到 document）

// 结论：原生事件先于 React 事件，React 事件最后到 document
```

**React 17 的变化**：
```javascript
// 点击 span，输出顺序：
// 2. button 原生
// 4. span React
// 3. button React
// 1. document 原生      （现在在 root 上）

// 如果 stopPropagation，document 上的事件不会执行（React 17）
```

## 总结

1. **合成事件**是 React 对原生事件的封装，提供跨浏览器兼容性
2. **React 17** 将事件委托从 document 改为 root 容器
3. **事件优先级**：离散事件（click）> 连续事件（mousemove）
4. **移除了事件池**：React 17 不再需要 `e.persist()`
5. **事件执行顺序**：原生事件 → React 事件 → 委托容器事件

## 相关源码文件

- `packages/react-dom/src/events/DOMPluginEventSystem.js` - 事件系统核心
- `packages/react-dom/src/events/ReactDOMEventListener.js` - 事件监听器
- `packages/react-dom/src/events/SyntheticEvent.js` - 合成事件对象

---

**上一篇**: [04 - Diff 算法和渲染优化](./04-Diff算法与渲染优化.md)
**下一篇**: [06 - 状态管理和 Context](./06-状态管理与Context.md)
